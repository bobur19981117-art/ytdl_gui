PySide6 YouTube Downloader GUI

1. Install dependencies:
   python -m pip install -r requirements.txt

2. Run:
   python main.py

The app uses yt-dlp for downloading. Default download folder: /storage/emulated/0/Download

For audio extraction, ffmpeg is recommended/required.
